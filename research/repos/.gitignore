# Ignore everything in these subdirectories
*
# But don't ignore directories themselves (so we can traverse them)
!*/
# Explicitly allow our research artifacts
!INDEX.md
!ANALYSIS.md
# Only un-ignore THIS specific .gitignore file, not nested ones
!/.gitignore
!Comparison*.md
